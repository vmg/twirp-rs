[package]
name = "twirp-rs"
version = "0.1.0"
authors = ["Vicent Marti <vmg@strn.cat>"]
edition = "2018"

[features]
service-gen = ["prost-build"]

[dependencies]
futures = "0.1"
hyper = "0.12"
prost = "0.4"
serde_json = "1.0"

prost-build = { version = "0.4", optional = true }
